[build-system]
requires = ["setuptools>=68", "versioningit"]
build-backend = "setuptools.build_meta"

[project]
name = "__COMPONENT_NAME__"
dynamic = ["version"]
description = "__DESCRIPTION__"
readme = "README.md"
requires-python = "__REQUIRES_PYTHON__"
license = {text = "MIT"}

[tool.versioningit]
default-version = "0.1.0+unknown"

[tool.versioningit.format]
distance = "{base_version}+{distance}.{vcs}{rev}"
dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"
distance-dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"

[tool.versioningit.vcs]
method = "git"
match = ["*"]
default-tag = "0.1.0"

[tool.versioningit.tag2version]
regex = '__VERSIONINGIT_PATTERN__'
require-match = true

[project.optional-dependencies]
test = [
  "pytest>=8",
  "ruff==__RUFF_VERSION__",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "__RUFF_TARGET__"
extend-exclude = ["MOLSYSSUITE_GUIDE.md"]

[tool.ruff.lint]
select = [__RUFF_RULES__]
